# Releases

Builds are published on the GitHub Releases page for this project.

The repository keeps the source code, docs, and game database. The Windows installer is attached to a release so people can download it without cloning the project.

If you build the app locally, the installer appears in `release/`. That folder is ignored by Git because it is generated output.
